var sphero = require('./index');

var client = sphero.createClient();
client.connect();
/*client.connect(function () {
  var run = function () {
    client.color('blue');
    setTimeout(function () {
      client.color('green');
      setTimeout(run, 400);
    }, 400);
  };

  run();
});*/

/*
client
.stack()
.connect()
.setRgbLedOutput('blue')
.wait(2)
.setRgbLedOutput('red')
*/